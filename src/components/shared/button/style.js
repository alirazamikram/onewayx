import styled from 'styled-components';

export const Wrapper = styled.div`

button {
    width: 250px;
    height: 45px;
    border-radius: 8px;
    border: 3px solid #ef3d24;
    background: rgba(239, 61, 36,0.5);
    color: white;
    font-family: var(--normal-font);
    font-weight: bold;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    font-size: 13px;
    cursor:pointer;
}

`;